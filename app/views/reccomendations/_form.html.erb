<%= form_with(model: reccomendation) do |form| %>
  <% if reccomendation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reccomendation.errors.count, "error") %> prohibited this reccomendation from being saved:</h2>

      <ul>
        <% reccomendation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
