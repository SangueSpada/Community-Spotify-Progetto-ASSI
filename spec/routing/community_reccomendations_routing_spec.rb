require "rails_helper"

RSpec.describe CommunityReccomendationsController, type: :routing do
=begin   describe "routing" do
    it "routes to #index" do
      expect(get: "/community_reccomendations").to route_to("community_reccomendations#index")
    end

    it "routes to #new" do
      expect(get: "/community_reccomendations/new").to route_to("community_reccomendations#new")
    end

    it "routes to #show" do
      expect(get: "/community_reccomendations/1").to route_to("community_reccomendations#show", id: "1")
    end

    it "routes to #edit" do
      expect(get: "/community_reccomendations/1/edit").to route_to("community_reccomendations#edit", id: "1")
    end


    it "routes to #create" do
      expect(post: "/community_reccomendations").to route_to("community_reccomendations#create")
    end

    it "routes to #update via PUT" do
      expect(put: "/community_reccomendations/1").to route_to("community_reccomendations#update", id: "1")
    end

    it "routes to #update via PATCH" do
      expect(patch: "/community_reccomendations/1").to route_to("community_reccomendations#update", id: "1")
    end

    it "routes to #destroy" do
      expect(delete: "/community_reccomendations/1").to route_to("community_reccomendations#destroy", id: "1")
    end
  end 
=end
end
